export const CANVAS_SIZE = 600;
export const CENTER = { x: CANVAS_SIZE / 2, y: CANVAS_SIZE / 2 };

export const ORBITS = [60, 120, 180]; // Radii of the three orbits
export const PLAYER_RADIUS = 8;
export const PLAYER_ANGULAR_SPEED = 0.05; // Base rotation speed

export const METEOR_SPEED_BASE = 2.0;
export const METEOR_SPAWN_INTERVAL = 1500; // ms
export const METEOR_RADIUS = 10;

export const SW_COOLDOWN_MS = 200; // Hardcore mode switch cooldown
export const NEAR_MISS_RADIUS = 30; // Distance to award combo points
export const SLOW_TIME_DURATION = 2000; // ms
export const SLOW_TIME_FACTOR = 0.3;

export const DIFFICULTY_INCREASE_INTERVAL = 10000; // Every 10 seconds
